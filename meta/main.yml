galaxy_info:
  author: Simon Schürg
  description: Role to install prerequisites needed for Ansible provisioning.
  # company: your company (optional)

  license: MIT
  min_ansible_version: 1.8
  github_branch: master

  platforms:
  - name: Fedora
    versions:
    - all
  - name: Ubuntu
    versions:
    - all
  - name: Debian
    versions:
    - all
  #- name: Alpine
  #  versions:
  #  - all
  #  - any
  - name: EL
    versions:
    - all

  galaxy_tags:
    - prerequisites
    - requirements
    - python
    - raw

dependencies: []
  # List your role dependencies here, one per line.
  # Be sure to remove the '[]' above if you add dependencies
  # to this list.
